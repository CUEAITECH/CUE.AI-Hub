# Nginx 配置片段：dev.hub.cueai.top → 本机 4318 端口
# 放到 /etc/nginx/sites-available/cue-hub-dev 后 symlink 到 sites-enabled
# 然后：sudo nginx -t && sudo systemctl reload nginx

server {
    listen 80;
    server_name dev.hub.cueai.top;

    # HTTPS 重定向（如果已配 SSL）
    # return 301 https://$host$request_uri;

    location / {
        proxy_pass         http://127.0.0.1:4318;
        proxy_http_version 1.1;

        # SSE / WebSocket 支持
        proxy_set_header   Upgrade    $http_upgrade;
        proxy_set_header   Connection "upgrade";
        proxy_set_header   Host       $host;
        proxy_set_header   X-Real-IP  $remote_addr;
        proxy_set_header   X-Forwarded-For $proxy_add_x_forwarded_for;

        # SSE 长连接：关闭缓冲，超时调长
        proxy_buffering    off;
        proxy_read_timeout 3600s;
        proxy_send_timeout 3600s;
    }
}

# HTTPS（certbot 会自动追加，或手动填入证书路径）
# server {
#     listen 443 ssl;
#     server_name dev.hub.cueai.top;
#     ssl_certificate     /etc/letsencrypt/live/dev.hub.cueai.top/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/dev.hub.cueai.top/privkey.pem;
#     location / { proxy_pass http://127.0.0.1:4318; ... }
# }
